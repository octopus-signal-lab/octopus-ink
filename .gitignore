# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build
/dist-desktop
/src-tauri/target
/src-tauri/gen

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (never commit secrets)
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# deploy / tooling (Vercel link + local agent state — never commit)
/.vercel
/.claude

# design handoff source assets (~9MB of duplicate binaries) — kept locally, not in repo.
# Remove this line if you want the Claude Design specs versioned.
/design-assts
